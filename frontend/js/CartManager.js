/**
 * Récupération des id des articles sélectionnés et ajout au panier (localstorage)
*/
console.log("CartManager is running !");

function getElementToAddToCart(elt, name, price){
    console.log(elt, name, price);
}